<f:UserControlBase x:Class="MyPoll.View.PollCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vw="clr-namespace:MyPoll.View"
             xmlns:f="clr-namespace:PRBD_Framework;assembly=PRBD_Framework"
             xmlns:vm="clr-namespace:MyPoll.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PollCardViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="80" d:DesignWidth="150">
    <StackPanel Margin="10,10,10,10">
        <!-- Ajouter une marge de 10 pixels en bas -->
        <TextBlock FontSize="14" FontWeight="Bold" Width="130" TextAlignment="Center" TextWrapping="Wrap">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0}, {1}">
                    <Binding Path="Name"/>
                    <Binding Path="Creator"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
    </StackPanel>

</f:UserControlBase>
