<f:UserControlBase x:Class="MyPoll.View.PollAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyPoll.View"
             xmlns:vw="clr-namespace:MyPoll.View"
             xmlns:f="clr-namespace:PRBD_Framework;assembly=PRBD_Framework"
             xmlns:vm="clr-namespace:MyPoll.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PollAddViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Grid.Row="0">
            <Run Text="{Binding Title, Mode=OneWay}" FontSize="18" FontWeight="Bold"/>
            <Run Text=" (" FontSize="12"/>
            <Run Text=" Je suis POLLADDVIEW ok created by " FontSize="12"/>
            <Run Text="{Binding Creator.Name}" FontSize="12" FontWeight="Bold"/>
            <Run Text=")" FontSize="12" />
        </TextBlock>
            
    </Grid>
</f:UserControlBase>
